import { Loading } from "@/components/ui/states/loading"

export default function AmbassadorMarketingLoading() {
  return (
    <Loading
      message="Chargement du kit marketing..."
      size="large"
      variant="spinner"
    />
  )
}
