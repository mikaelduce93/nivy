# Auth + Onboarding compliance

## Score: 48/100  (was 22 — Wave 1A landed: AUTH-001, AUTH-003, AUTH-004 fixed; PARENT-005 fixed)

> Wave 1A — Identity Truth (2026-05-08) closed the four highest-impact auth
> P0s on the launch path: profiles.role enum is now CHECK-constrained to the
> canonical 7 values, the validate-teen + parent/teens/create routes are
> rebuilt around supabase.auth.admin.createUser with rollback on failure,
> /auth/redirect implements the §3 LOCKED truth table including mentor +
> driver + status-aware sub-routing. Migration 094 also adds is_onboarded,
> admin_roles.permissions JSONB, parent_teen_links.status, and the singular
> `audit_log` table.

## Launch status: BLOCKED — the canonical identity invariant (`auth.users.id == profiles.id`, no `profiles` row without an `auth.users` row, role assigned via `handle_new_user` trigger from `raw_user_meta_data.role`) is broken in three of the four account-creation surfaces. Teens validated by their parent get a `profiles` row with no `auth.users` mate (locked out forever). Partners registered via `/api/partners/register` cannot log in (no `auth.users` created). Ambassador candidature writes directly to a status-pending table that the admin approval flow does not flip back into a role. The marketing wizard runs a parallel `auth.signUp` call. The role enum is missing four canonical values (`mentor`, `driver`, `ambassador`, `admin` per `profiles.role` CHECK) — the redirect switch silently dumps `mentor`/`driver` users back to the pre-account marketing showcase. `is_onboarded` exists as a column for teens only and there is no middleware gate enforcing wizard completion. Two of the seven canonical signup entry points (`/devenir-mentor/candidature`, `/devenir-chauffeur/candidature`) and the entire `/driver/*` surface do not exist. No launch is possible until these are fixed; partial fixes (e.g. ship parent + teen flows clean) could enable a closed BETA on parent-only invites.

## Findings count: P0=12, P1=8, P2=5, P3=2 (total 27)

## Top 3 P0

1. **CANON-AUTH-001** — `app/api/auth/validate-teen/route.ts:184-196` inserts a `profiles` row for the teen WITHOUT calling `supabase.auth.admin.createUser` first. The teen has no `auth.users` row, no password, no magic-link — they cannot ever log in. The placeholder email pattern `teen_${registration.id}@teensparty.local` is the literal forbidden anti-pattern from canon §6 #6. The `profiles.id == auth.users.id` invariant (canon §6 #5, whitepaper §20 LOCKED) is violated by autogenerated UUID.

2. **CANON-AUTH-002** — `app/api/partners/register/route.ts:29-50` directly inserts into `partners` (no `profiles` row, no `auth.users` row). Per canon §1 row "partner" + §7 M10, the route MUST atomically (a) `supabase.auth.admin.createUser` for `contact_person_email`, (b) trigger creates `profiles` row with `role='partner'`, (c) insert `partners` row, (d) email magic-link. Today none of (a)/(b)/(d) happens — the partner has no way to authenticate to the dashboard the email confirmation invites them to.

3. **CANON-AUTH-003** — `app/auth/redirect/page.tsx:43-62` switch is missing `mentor` and `driver` cases entirely; the default branch sends every unknown role back to `/onboarding` (the pre-account marketing wizard). It also lacks status-aware sub-routing (partners-pending → `<PartnerAwaitingApproval />`, mentor KYC tier flips, ambassador approval state, `is_onboarded` gating). Direct contradiction with §3 LOCKED switch table.

## Findings

```json
{
  "id": "CANON-AUTH-001",
  "domain": "auth-onboarding",
  "severity": "P0",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§1 'teen' row: validate-teen route MUST call supabase.auth.admin.createUser({email: teen_email, email_confirm:true}) and use the returned uid as profiles.id and teens.id. §6 FORBIDDEN #1: 'No profiles row creation without an auth.users row.' §6 FORBIDDEN #5: 'profiles.id MUST equal auth.users.id.' §6 FORBIDDEN #6: 'No teen_<id>@teensparty.local placeholder email.'",
  "code_files": [
    "app/api/auth/validate-teen/route.ts:184-196",
    "app/api/auth/validate-teen/route.ts:188"
  ],
  "current_behavior": "On parent approval the route inserts directly into public.profiles with autogenerated uuid (teenProfile.id) and email = teen_email OR fallback `teen_${registration.id}@teensparty.local`. No supabase.auth.admin.createUser call. No magic-link / set-password email is sent to the teen. The `teens` row (line 207-223 inserts into teen_full_profile, not teens) is FK'd to a profile id that has no matching auth.users row. The teen literally has no credentials and cannot authenticate. is_onboarded is never set to false explicitly (relies on default).",
  "why_it_matters": "The product's core 'parent invites teen' flow produces a phantom teen account. The teen receives nothing actionable; the parent sees a fake-success toast. This is the single largest fake-success in the codebase: the marketing wizard, the parent dashboard, and the email all promise a teen login that does not exist. Also corrupts the database: orphan profiles rows (already 4 found in db-integrity audit), broken FK invariant.",
  "recommended_fix": "Rewrite POST /api/auth/validate-teen approval branch: (1) require teen_email on registration (or canonicalise via parent-supplied alias); (2) call `supabase.auth.admin.createUser({ email: teen_email, email_confirm: true, user_metadata: { role: 'teen', full_name } })`; (3) let handle_new_user trigger create the profiles row (do NOT insert manually); (4) `INSERT INTO teens (id, ...) VALUES (newUid, ...)`; (5) `INSERT INTO parent_teen_links`; (6) `supabase.auth.admin.generateLink({ type: 'magiclink', email })` and email it to the teen; (7) set profiles.is_onboarded=false explicitly. Drop the `teen_${registration.id}@teensparty.local` fallback entirely — refuse the request if teen_email is null.",
  "blast_radius": "High — touches the only path teen accounts are created today. Requires migration to add teen_email NOT NULL on pending_teen_registrations + UI change in /onboarding/teen-setup-step to collect it.",
  "implementation_complexity": "Medium",
  "test_required": [
    "POST /api/auth/validate-teen with action=approve creates auth.users row, profiles.id == auth.users.id, teens.id == auth.users.id, parent_teen_links row exists",
    "Teen receives magic-link email; clicking it lets them log in",
    "Request without teen_email returns 400 (no fallback placeholder used)",
    "profiles.is_onboarded = false after approval; teen lands on /onboarding/interests on first login",
    "No orphan profiles row remains after a failed mid-flow rollback"
  ],
  "status": "fixed-2026-05-08"
}
```

```json
{
  "id": "CANON-AUTH-002",
  "domain": "auth-onboarding",
  "severity": "P0",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§1 'partner' row: POST /api/partners/register MUST atomically (a) supabase.auth.admin.createUser for contact_person_email, (b) insert profiles row with role='partner' (via trigger), (c) insert partners row with status='pending', (d) email magic-link. §7 MISSING M10. §6 FORBIDDEN #1.",
  "code_files": [
    "app/api/partners/register/route.ts:29-50",
    "app/api/partners/register/route.ts:78-85"
  ],
  "current_behavior": "Route writes only to partners table. No supabase.auth.admin.createUser. No profiles row. No magic-link sent. Returns success { partnerId } and the user receives no credentials. The dashboard at /partner gates on `email = profile.email` (lib/auth/get-user-role.ts:148) but the partner has no profile. Also writes columns (company_registration_number, tax_id, phone, website, address, city, postal_code, description, contact_person_*) that per `partner-network.md` §3 do not exist on the live partners table — silent data loss compounds the issue (CONTRADICTION C10).",
  "why_it_matters": "Partners cannot log in. The 4-step Wave 1 wizard produces a black-box record visible only to admins. From the partner's POV the funnel ends in dead air. Cannot ship the partner ecosystem without this; combined with admin approval flow that also doesn't flip role, partners are perma-locked-out.",
  "recommended_fix": "Refactor route: (1) `supabase.auth.admin.createUser({ email: contact_person_email, email_confirm: false, user_metadata: { role: 'partner', full_name: contact_person_name } })`; (2) handle_new_user trigger lands profiles row with role='partner'; (3) INSERT INTO partners (..., status='pending', profile_id=newUid); (4) `supabase.auth.admin.generateLink({ type: 'magiclink' | 'invite' })` → email contact_person_email with set-password link. Wrap (1)+(3) in a SECURITY DEFINER RPC `register_partner` that rolls back auth.users if partners insert fails. Also gate column writes behind a migration that adds the missing columns OR drops them from the payload.",
  "blast_radius": "High — touches partner onboarding end-to-end + needs partners table migration (C10). Also affects /partner dashboard which today queries by partners.email instead of profiles.id (smell — should query by partners.profile_id once that FK exists).",
  "implementation_complexity": "Medium",
  "test_required": [
    "POST /api/partners/register creates auth.users + profiles (role=partner) + partners (status=pending)",
    "contact_person_email receives magic-link / set-password email",
    "Partner can log in; /auth/redirect routes to /partner with <PartnerAwaitingApproval />",
    "All form columns persist (RC, ICE, RIB, phone, address, etc.) — no silent data loss",
    "Failure mid-transaction rolls back auth.users (no orphan)"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-003",
  "domain": "auth-onboarding",
  "severity": "P0",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§3 LOCKED — /auth/redirect switch table. MUST handle mentor (kyc tier flips), driver (kyc states), ambassador (status), partner (status), is_onboarded gates, and fall through to /auth/error?reason=unknown_role (NEVER /onboarding).",
  "code_files": [
    "app/auth/redirect/page.tsx:43-62"
  ],
  "current_behavior": "Switch only handles teen, parent, ambassador, partner, admin. No mentor case → mentor users hit default → router.push('/onboarding') (the pre-account marketing wizard). No driver case at all. No status-aware logic for partners (active/pending → same /partner). No is_onboarded gating anywhere. Direct contradiction with `lib/auth/get-user-role.ts:206-223` getDashboardPath() which DOES return /mentor/dashboard for mentor (CONTRADICTION C2).",
  "why_it_matters": "Every authenticated mentor or driver is sent back to a pre-account showcase that has no track for them — they cannot reach their dashboard. Every authed user without a role row is sent to /onboarding which contains the forbidden auth.signUp call (CANON-AUTH-005). Status-aware redirect gaps mean a partner with status='pending' lands on the live dashboard instead of <PartnerAwaitingApproval/>. is_onboarded=false users can skip wizards by typing the dashboard URL directly.",
  "recommended_fix": "Replace switch with the exact §3 LOCKED table. Read `profile.role`, `profile.is_onboarded`, plus per-role status (partners.status, mentors.status, mentors.kyc_status, nivy_drivers.kyc_status, ambassadors.status). Compose the destination per the truth table. Default branch = `router.replace('/auth/error?reason=unknown_role')`. Move logic into a server-side route handler (e.g. /auth/redirect/route.ts → 302 Location) so it never flashes a client-side intermediate state.",
  "blast_radius": "Medium — single page rewrite, but it's the central post-login choke point.",
  "implementation_complexity": "Medium",
  "test_required": [
    "Mentor with kyc_status=submitted → /mentor/onboarding/kyc-pending",
    "Mentor with status=intro_only AND is_onboarded=false → /mentor/onboarding/profile",
    "Driver with kyc_status=approved AND is_onboarded=true → /driver/dashboard",
    "Partner with status=pending → /partner (awaiting approval banner)",
    "Ambassador with status=pending → /devenir-ambassadeur status banner",
    "Profile with role NULL → /onboarding (showcase, role-pick)",
    "Profile with role='influencer' (not in enum) → /auth/error?reason=unknown_role (not /onboarding)",
    "Parent with is_onboarded=false → /onboarding/parent (NOT /parent)"
  ],
  "status": "fixed-2026-05-08"
}
```

```json
{
  "id": "CANON-AUTH-004",
  "domain": "auth-onboarding",
  "severity": "P0",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§2 LOCKED — profiles.role enum MUST be `parent | teen | partner | mentor | driver | ambassador | admin`. §7 MISSING M19: live DB only stores `parent | partner | teen`.",
  "code_files": [
    "gamification-system/database/all_migrations.sql:8",
    "lib/auth/get-user-role.ts:3",
    "lib/auth/admin-permissions.ts:99"
  ],
  "current_behavior": "profiles table is created as `role TEXT DEFAULT 'parent'` with NO CHECK constraint. Live data observed in vision/audit-prelaunch/01-db-integrity.md confirms only parent | partner | teen present. UserRole TS union (lib/auth/get-user-role.ts:3) lists `parent|teen|ambassador|admin|super_admin|moderator|support|partner|mentor|unknown` — union has 4 values that are NOT in any DB enum (admin/super_admin/moderator/support/mentor) and conflates admin sub-roles (super_admin/moderator/support which canon §2 says belong to admin_roles.role) with profile roles. Every gate that does `userInfo.role === 'mentor' | 'driver' | 'ambassador' | 'admin'` returns false because that string is never written to the column.",
  "why_it_matters": "Until the enum migration ships, any code that branches on the four missing role values is dead — including the redirect switch fix above. Mentor/driver/ambassador funnels cannot complete role assignment. lib/auth/admin-permissions.ts:99 trusts profile.role against ['super_admin','admin','moderator','support'] which is structurally wrong: per canon admin sub-roles live in admin_roles table; the only profile.role admin marker is 'admin'.",
  "recommended_fix": "Ship migration: `ALTER TABLE public.profiles ADD CONSTRAINT profiles_role_chk CHECK (role IN ('parent','teen','partner','mentor','driver','ambassador','admin')); ALTER TABLE public.profiles ALTER COLUMN role SET DEFAULT 'parent'; ALTER TABLE public.profiles ADD COLUMN IF NOT EXISTS is_onboarded BOOLEAN NOT NULL DEFAULT FALSE;` Then refactor lib/auth/admin-permissions.ts:99 to check `profile.role='admin'` only, then read sub-role from admin_roles.role. Refactor UserRole union to drop super_admin/moderator/support (move to AdminSubRole type).",
  "blast_radius": "High — every role-gated route. Must be coordinated with the redirect rewrite (CANON-AUTH-003) and missing-role data migration for any pre-existing partners/teens that have inconsistent values.",
  "implementation_complexity": "Medium",
  "test_required": [
    "Migration is idempotent",
    "INSERT INTO profiles with role='influencer' fails CHECK",
    "INSERT INTO profiles with role='mentor' succeeds",
    "lib/auth/get-user-role.ts UserRole union compiles to canonical 7 values",
    "Existing rows with role IN ('super_admin','moderator','support') (if any) are migrated to role='admin' + admin_roles.role=<sub>"
  ],
  "status": "fixed-2026-05-08"
}
```

```json
{
  "id": "CANON-AUTH-005",
  "domain": "auth-onboarding",
  "severity": "P0",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§5 DEPRECATED + §6 FORBIDDEN #3: No `auth.signUp` outside `app/auth/sign-up/page.tsx`. Marketing wizard MUST NOT call auth.signUp; its 'Continue' must redirect to /auth/sign-up?source=wizard&tempUserId=… so XP merges post-signup.",
  "code_files": [
    "components/onboarding/parent-setup-step.tsx:88-99",
    "components/onboarding/parent-setup-step.tsx:108-113"
  ],
  "current_behavior": "ParentSetupStep calls `supabase.auth.signUp({ email, password, options: { data: { user_type:'parent', ... } } })` directly inside the wizard step, then ALSO inserts into a `parents` table (line 108) — which is a separate from `profiles`. This creates a second signup path parallel to /auth/sign-up; the 'parents' table doesn't appear in canon and is itself a data-model smell.",
  "why_it_matters": "Two consequences: (a) Dual-signup smell — a parent who arrives via /onboarding hits a different validation pipeline than via /auth/sign-up (different fields collected, different metadata sent to handle_new_user trigger, no role='parent' set in raw_user_meta_data → trigger may default-land them as 'parent' but data shape differs); (b) XP-on-signup merge mechanism breaks because the two wizards don't hand off tempUserId. Direct violation of §6 FORBIDDEN #3.",
  "recommended_fix": "Delete the auth.signUp call in parent-setup-step.tsx. Replace 'Continue' button handler with `router.push('/auth/sign-up?source=wizard&tempUserId=' + tempUserId)`. The wizard's pre-account XP/badges live in `onboarding_progress` keyed by tempUserId; sync-on-signup is performed by handle_new_user trigger reading raw_user_meta_data.tempUserId. Drop the `parents` table insert entirely (parent profile data lives in `profiles`).",
  "blast_radius": "Medium — wizard UX changes mid-flow, but eliminates the dual-signup bug entirely.",
  "implementation_complexity": "Low",
  "test_required": [
    "Wizard 'Continue' button on parent track redirects to /auth/sign-up?source=wizard&tempUserId=…",
    "No auth.signUp call in components/onboarding/**",
    "ESLint rule rejects auth.signUp imports outside app/auth/sign-up/page.tsx",
    "tempUserId XP/badges merge into the post-signup teen/parent profile via trigger"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-006",
  "domain": "auth-onboarding",
  "severity": "P0",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§4 LOCKED + §7 M14: profiles.is_onboarded BOOLEAN NOT NULL DEFAULT FALSE column required; only POST /api/<role>/onboarding/complete (×6 roles) may flip to true; middleware MUST gate any authed user with is_onboarded=false to their wizard.",
  "code_files": [
    "middleware.ts:221-308",
    "app/api/teen/onboarding/complete/route.ts",
    "app/onboarding/complete/page.tsx:28-35"
  ],
  "current_behavior": "Only teen has a complete endpoint (`POST /api/teen/onboarding/complete`). No parent / partner / mentor / driver / ambassador equivalents (canon §4 mandates 6). Middleware (middleware.ts:221-308) gates protected routes on auth + role match, but NEVER reads is_onboarded — so a parent with is_onboarded=false lands on /parent directly, skipping the parent wizard. The is_onboarded column itself is referenced in code but per canon §7 M14 + db-integrity audit, the column does not exist live (or exists only because a migration partially added it). app/onboarding/complete/page.tsx writes is_onboarded directly from a server component (line 30-35) bypassing the dedicated API endpoint — duplicates state-mutation paths.",
  "why_it_matters": "The whole 'wizard before dashboard' contract is fictional. Parents skip CGU e-signature, top-up, spend-mode (P0 minor-protection compliance gap). Partners skip KYC upload + first-offer + scanner setup. Mentors skip availability publication. Without an is_onboarded gate, the wizards exist as decoration — any URL-typed user is on the dashboard immediately. CNDP / minor-protection posture is broken because parental e-signature is not enforced as a precondition.",
  "recommended_fix": "(1) Migration: confirm column exists, force NOT NULL DEFAULT false, backfill all existing rows to true (assume legacy users have completed informally). (2) Build POST /api/<role>/onboarding/complete for parent, partner, mentor, driver, ambassador, with hard preconditions (e_signatures.signed_at NOT NULL for parent; kyc_documents approved for partner; etc). (3) Middleware: after fetching profile, if is_onboarded=false AND path NOT IN [/auth/*, /onboarding/*, /api/*, /_next/*] → redirect to role-specific wizard root. Admin role bypasses. (4) Reset on role change via DB trigger. (5) Remove direct is_onboarded UPDATE in app/onboarding/complete/page.tsx; have it POST to /api/teen/onboarding/complete instead.",
  "blast_radius": "High — middleware change affects every protected route + every role.",
  "implementation_complexity": "High",
  "test_required": [
    "Parent with is_onboarded=false → /parent → 302 to /onboarding/parent/e-signature",
    "Teen with is_onboarded=false → /teen → 302 to /onboarding/interests",
    "Mentor with is_onboarded=false → /mentor/dashboard → 302 to /mentor/onboarding/profile",
    "Admin role bypasses is_onboarded check entirely",
    "POST /api/parent/onboarding/complete fails 412 if e_signatures.signed_at NULL",
    "POST flips is_onboarded=true atomically",
    "Role change UPDATE on profiles resets is_onboarded=false"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-007",
  "domain": "auth-onboarding",
  "severity": "P0",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§7 M1 + §1 'mentor' row: /devenir-mentor + /devenir-mentor/candidature MISSING — must be built. §6 FORBIDDEN #8: 'No call our API endpoint copy in user-facing UI' — current mentor empty state literally instructs users to call /api/mentor/apply.",
  "code_files": [
    "app/api/mentor/apply/route.ts",
    "app/devenir-mentor/"
  ],
  "current_behavior": "No app/devenir-mentor/ directory exists. The only entrypoint is POST /api/mentor/apply (route.ts:14 calls RPC apply_mentor) which expects an already-authed user.id. There is no candidature page, no KYC upload UI (CIN, selfie, casier judiciaire, 2 references, intro video), no /mentor/onboarding/* wizard pages, no /mentor/availability page (M4). UserRole.mentor is in the TS union but profiles.role enum does not contain 'mentor' (CANON-AUTH-004) so even successful apply_mentor RPC cannot legally land role='mentor'.",
  "why_it_matters": "Mentor surface is 0% reachable from public marketing. Even an admin manually flipping a profile to role='mentor' would (a) break CHECK constraint once enum is enforced and (b) leave the mentor with no wizard to run. The candidature → KYC → tier-flip → first-availability chain (canon §4 + U2 founder ruling) cannot exist.",
  "recommended_fix": "Build M1 (candidature page) + M5 (mentor wizard) + M6 (KYC upload UI) per §1 'mentor' row + §4. Auth-gate /devenir-mentor/candidature: if anonymous, redirect to /auth/sign-up?role=mentor&redirect=/devenir-mentor/candidature. Form posts to /api/mentor/apply (extended to accept KYC document URLs). apply_mentor RPC must (a) confirm auth.users row exists, (b) UPDATE profiles.role='mentor' (requires CANON-AUTH-004 enum migration first), (c) INSERT mentors (status='pending', kyc_status='submitted'), (d) is_onboarded=false. Then build /mentor/onboarding/{kyc-pending,profile,availability}.",
  "blast_radius": "High — entirely new surface area (UI + API + storage buckets for KYC).",
  "implementation_complexity": "High",
  "test_required": [
    "Anonymous visit to /devenir-mentor/candidature → /auth/sign-up?role=mentor&redirect=…",
    "Authed parent submitting form: profile.role flips to 'mentor', mentors row created with kyc_status='submitted'",
    "/auth/redirect with mentor + kyc_status='submitted' → /mentor/onboarding/kyc-pending",
    "KYC docs persisted to private bucket with signed-URL only access",
    "Tier flip pending → intro_only by admin opens /mentor/onboarding/profile"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-008",
  "domain": "auth-onboarding",
  "severity": "P0",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§7 M2 + M3 + §1 'driver' row: /devenir-chauffeur + /devenir-chauffeur/candidature + entire /driver/* surface MISSING; POST /api/driver/apply MISSING; POST /api/driver/onboarding/complete MISSING. INDEX founder decision F2: driver is first-class profiles.role.",
  "code_files": [
    "app/api/driver/",
    "app/devenir-chauffeur/",
    "app/driver/"
  ],
  "current_behavior": "app/api/driver/ contains only ride dispatch/track/complete routes (driver-side ride operations) — no apply, no onboarding/complete. No app/devenir-chauffeur/ directory. No app/driver/dashboard or /driver/* pages. UserRole TS union doesn't include 'driver' (lib/auth/get-user-role.ts:3 — confirms canon CONTRADICTION C2 partial). nivy_drivers table referenced in canon but no creation flow.",
  "why_it_matters": "An entire founder-decided first-class role (F2) does not exist in the product. Cannot operate the transport vertical with in-house drivers (whitepaper §19.4.2 hybrid model). All ride dispatch APIs that exist would 401/404 on driver auth.",
  "recommended_fix": "Build the full driver surface: /devenir-chauffeur (public marketing), /devenir-chauffeur/candidature (auth-gated KYC form), POST /api/driver/apply (creates auth.users + role='driver' + nivy_drivers row with kyc_status='pending'), /driver/onboarding/{kyc-pending,setup} pages, /driver/dashboard, POST /api/driver/onboarding/complete. Add 'driver' to UserRole TS union. Order-of-operations: ship after CANON-AUTH-004 enum migration so role='driver' is valid.",
  "blast_radius": "High — net-new surface area + KYC private bucket + middleware route gating.",
  "implementation_complexity": "High",
  "test_required": [
    "/devenir-chauffeur/candidature renders for anonymous (or redirects to signup)",
    "POST /api/driver/apply creates auth.users + profiles(role='driver') + nivy_drivers(kyc_status='pending')",
    "/auth/redirect routes driver per §3 LOCKED table",
    "/driver/dashboard requires kyc_status='approved' AND is_onboarded=true",
    "Middleware blocks non-driver from /driver/*"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-009",
  "domain": "auth-onboarding",
  "severity": "P0",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§7 M13 + §1 'ambassador' row: POST /api/ambassador/apply route required; admin approval MUST atomically flip both ambassadors.status='active' AND profiles.role='ambassador' (single transaction). §6 FORBIDDEN #2: role assignment requires profiles row, role updates audited.",
  "code_files": [
    "components/ambassador-application-form.tsx:51-59",
    "app/devenir-ambassadeur/candidature/page.tsx:17-24"
  ],
  "current_behavior": "Form (components/ambassador-application-form.tsx) calls supabase.from('ambassadors').insert({ profile_id: profile.id, ..., status: 'pending' }) directly from the client. There is NO POST /api/ambassador/apply route (Glob returns only withdrawals + shop). Admin-side approval (not located here, but by canon §1 ambassador must atomically flip role + status — needs verification). Direct client insert bypasses any RPC / audit trail; ambassadors table writes still require RLS to be configured (audit risk).",
  "why_it_matters": "(a) No server-side validation of audience_size, primary_platform, payout_method, parental_consent for under-18s. Minor-targeted growth surface with cash payouts shipped without audit trail. (b) Ambassador role is never assigned because client cannot UPDATE profiles.role due to RLS; even if admin approves status='active', without an atomic role flip, /auth/redirect will route them as 'parent' / 'teen' to the wrong dashboard. (c) Client-side application means no anti-spam, no rate-limit, no input sanitisation server-side.",
  "recommended_fix": "(1) Build POST /api/ambassador/apply route — server-side withSecurity wrapper, validates payload (incl. parental_consent for under-18 via teens.date_of_birth lookup), inserts ambassadors row with status='pending'. (2) Replace client direct insert in components/ambassador-application-form.tsx with fetch('/api/ambassador/apply'). (3) Audit /admin/ambassadeurs approval handler — must call SECURITY DEFINER RPC `approve_ambassador(p_ambassador_id, p_admin_id)` that atomically: UPDATE ambassadors.status='active' + UPDATE profiles.role='ambassador' + INSERT audit_log row + INSERT referral_codes row (whitepaper attribution). All in one transaction.",
  "blast_radius": "Medium — single API + single SQL function.",
  "implementation_complexity": "Medium",
  "test_required": [
    "POST /api/ambassador/apply rejects under-18 without parental_consent payload",
    "Approval RPC flips both ambassadors.status='active' AND profiles.role='ambassador' atomically",
    "Failure in either UPDATE rolls back the other",
    "audit_log row written with admin_id, action='ambassador_approved'",
    "referral_codes row created with is_active=true",
    "Post-approval /auth/redirect routes user to /onboarding/ambassador (is_onboarded=false reset by role-change trigger)"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-010",
  "domain": "auth-onboarding",
  "severity": "P0",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§4 LOCKED — parent wizard required steps: (1) e-signature, (2) add first teen, (3) first top-up (or skip), (4) spend mode. Routes: /onboarding/parent/{e-signature,add-teen,topup,spend-mode}. POST /api/parent/onboarding/complete sets is_onboarded=true. §7 M7 + M8 — wizard chain MISSING + POST /api/parent/e-signature endpoint MISSING.",
  "code_files": [
    "app/onboarding/",
    "app/api/parent/"
  ],
  "current_behavior": "Glob `app/onboarding/parent/**` returns nothing. The four wizard pages do not exist as a chained flow. Per audit reference, only GET /api/parent/e-signature/status exists (not the POST that persists a signature). is_onboarded for parent role is never flipped (CANON-AUTH-006).",
  "why_it_matters": "(a) Parents arrive at /parent dashboard without ever signing CGU — CNDP filing risk, minor-consent risk. (b) No first top-up flow; spend-mode (budget caps) never chosen — parental control posture is fictional. (c) Per founder ruling on /onboarding (U11): wizard is 'pre-account showcase only' but the parent wizard is the post-account onboarding — these are different flows that must both exist; today neither exists for parent post-signup.",
  "recommended_fix": "Build /onboarding/parent/e-signature → /onboarding/parent/add-teen → /onboarding/parent/topup → /onboarding/parent/spend-mode → POST /api/parent/onboarding/complete. Build POST /api/parent/e-signature that captures the signature blob (canvas drawing or scrypt-hashed type-name), inserts e_signatures(parent_id, signed_at, ip, user_agent, signature_blob_url), returns success. The add-teen step calls existing POST /api/parent/teens/invite (or M18 linking-code variant). Top-up step uses the canonical {teenId, amount_dh} payload (CANON-ECON-001 prerequisite). Spend-mode persists family_settings rows.",
  "blast_radius": "High — net-new wizard surface + e_signatures table + spend-mode persistence.",
  "implementation_complexity": "High",
  "test_required": [
    "Parent post-signup → /auth/redirect → /onboarding/parent/e-signature (because is_onboarded=false)",
    "POST /api/parent/e-signature persists row with signed_at NOT NULL",
    "Cannot reach /parent/spend-mode without completing e-signature step (412 from /api/parent/onboarding/complete)",
    "Top-up step is skippable (writes skipped_at)",
    "POST /api/parent/onboarding/complete returns 412 if e_signatures.signed_at NULL",
    "Successful complete sets is_onboarded=true and redirects to /parent"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-011",
  "domain": "auth-onboarding",
  "severity": "P0",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§2 LOCKED — admin sub-roles live in admin_roles.role; profiles.role for any admin is always 'admin' (single value). §7 M19: admin_roles.permissions JSONB column MISSING in live DB.",
  "code_files": [
    "lib/auth/admin-permissions.ts:90-120",
    "lib/auth/admin-permissions.ts:99",
    "lib/auth/get-user-role.ts:182-197"
  ],
  "current_behavior": "lib/auth/admin-permissions.ts:99 reads profile.role and tests against ['super_admin','admin','moderator','support']. This means admin sub-roles must be written into profiles.role — directly contradicting canon §2 which puts the sub-role in admin_roles.role and keeps profiles.role='admin'. Then line 103-108 reads admin_roles.permissions but per canon §7 M19 + CONTRADICTION C9, admin_roles.permissions JSONB column does not exist in live DB. Silent no-op on per-admin overrides.",
  "why_it_matters": "(a) Permission model is structurally wrong: superadmin/moderator/support flow only works if a profile is given role='super_admin' which violates the canonical 7-value enum (CANON-AUTH-004). Once that enum is migrated, every admin breaks. (b) Custom permission overrides (line 118-119 admin_role.permissions object merge) silently no-op because the column doesn't exist — admins think a deny override is in place when it isn't.",
  "recommended_fix": "(1) Migration: ALTER TABLE admin_roles ADD COLUMN IF NOT EXISTS permissions JSONB NOT NULL DEFAULT '{}'::jsonb. (2) Refactor getAdminInfo: a) check profile.role='admin' first (single value), b) read sub-role from admin_roles.role, c) compute permissions from sub-role, d) merge admin_roles.permissions overrides. (3) Migration: any existing profiles.role IN ('super_admin','moderator','support') → flip to profiles.role='admin' + ensure admin_roles.role contains the sub-value.",
  "blast_radius": "Medium — two-table migration + lib refactor.",
  "implementation_complexity": "Medium",
  "test_required": [
    "Migration adds permissions JSONB column (idempotent)",
    "getAdminInfo reads sub-role from admin_roles.role, not profiles.role",
    "Admin with admin_roles.permissions = { 'analytics.financial': false } cannot view financial analytics even though sub-role='admin'",
    "profile.role='admin' without admin_roles row → getAdminInfo returns null"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-012",
  "domain": "auth-onboarding",
  "severity": "P0",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§5 DEPRECATED + §6 FORBIDDEN #1 + §1 'partner' row admin approval atomic transaction: 'Direct insert into profiles from app/api/partners/register without auth.users → Replace with supabase.auth.admin.createUser first'.",
  "code_files": [
    "app/api/auth/validate-teen/route.ts:184-204",
    "app/api/partners/register/route.ts:29-50"
  ],
  "current_behavior": "Two routes insert into public.profiles directly without an auth.users mate (covered structurally in CANON-AUTH-001 + CANON-AUTH-002). Per canon §6 FORBIDDEN #1: 'Every INSERT INTO public.profiles must FK to an existing auth.users.id. Use supabase.auth.admin.createUser first; rely on the handle_new_user trigger; never INSERT INTO profiles (...) from app code.' Both routes do exactly that. db-integrity.md observed 4 orphan profiles rows in production already.",
  "why_it_matters": "Tracking issue for the orphan-profiles invariant. Even with CANON-AUTH-001 + 002 fixed, the codebase needs a CI rule banning `INSERT INTO public.profiles` from app code AND the existing orphan rows must be cleaned (DELETE FROM profiles p WHERE NOT EXISTS (SELECT 1 FROM auth.users u WHERE u.id = p.id)).",
  "recommended_fix": "(a) After CANON-AUTH-001 + 002 land, add CI lint: forbid `from('profiles').insert` and `from(\"profiles\").insert` regex outside dedicated admin tooling. Forbid raw SQL `INSERT INTO public.profiles` outside migrations. (b) One-shot cleanup migration to delete orphan profiles. (c) Verify handle_new_user trigger exists in current migrations and creates the row from auth.users insert (per IMPLEMENTATION_RECETTE.md migration 040 it does — but no `handle_new_user` reference found in gamification-system/database/** ripgrep — verify the live DB).",
  "blast_radius": "Low — invariant tracking + cleanup. The actual breakage is in CANON-AUTH-001 + 002.",
  "implementation_complexity": "Low",
  "test_required": [
    "INSERT INTO profiles VALUES (random_uuid, ...) blocked by FK constraint",
    "ripgrep `from\\(['\\\"]profiles['\\\"]\\)\\.insert` returns no app code matches",
    "Cleanup migration deletes 4+ orphan rows",
    "handle_new_user trigger exists and fires on auth.users INSERT in live DB"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-013",
  "domain": "auth-onboarding",
  "severity": "P1",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§6 FORBIDDEN #4: 'No app/auth/sign-up form that excludes role.' The form MUST set raw_user_meta_data.role from a query-param ?role= (default parent) so the trigger lands the right row.",
  "code_files": [
    "app/auth/sign-up/page.tsx:71-84"
  ],
  "current_behavior": "supabase.auth.signUp call passes `data: { nom, prenom, telephone, ville, accept_newsletter }` — no `role` field. So handle_new_user trigger reads raw_user_meta_data->>'role' as NULL and falls back to default 'parent'. This is fine for the parent path but the canon mandates supporting `?role=mentor|driver|ambassador` from the candidature redirect chain (§5 DEPRECATED row 4: deprecated /auth/sign-up for partner/mentor/driver/ambassador funnels redirect into the canonical sign-up form carrying a role).",
  "why_it_matters": "Once /devenir-mentor/candidature (CANON-AUTH-007) and /devenir-chauffeur/candidature (CANON-AUTH-008) ship, they will redirect anonymous visitors to /auth/sign-up?role=mentor for signup-then-back-to-candidature. Today that ?role= is silently discarded → user lands as parent and the candidature page sees a parent profile.role and fails the role gate.",
  "recommended_fix": "Read URL searchParams.get('role') (whitelist against the §2 enum minus admin), include in raw_user_meta_data.role; surface the role choice in the heading copy ('Inscription mentor' / 'Inscription chauffeur'); preserve the redirect target via ?redirect=…. Add ?source=wizard&tempUserId=… handling per CANON-AUTH-005.",
  "blast_radius": "Low — single page change.",
  "implementation_complexity": "Low",
  "test_required": [
    "/auth/sign-up?role=mentor → auth.users.raw_user_meta_data.role='mentor' → handle_new_user creates profiles row with role='mentor'",
    "/auth/sign-up (no role) → defaults to 'parent'",
    "/auth/sign-up?role=admin → rejected (admins are not publicly creatable)",
    "/auth/sign-up?role=influencer → rejected (not in enum)",
    "?redirect=/devenir-mentor/candidature is honoured after email confirmation"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-014",
  "domain": "auth-onboarding",
  "severity": "P1",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§3 LOCKED partner row: partners.status ∈ {pending,in_review,active,rejected,suspended} (collapsed from {active,verified,approved}). U10 ruling = collapse. §2: PARTNER_ACTIVE_STATUSES = {active, verified, approved} is a smell — collapse verified and approved into active.",
  "code_files": [
    "app/partner/page.tsx:23"
  ],
  "current_behavior": "const PARTNER_ACTIVE_STATUSES = new Set(['active', 'verified', 'approved']) — three synonymous values treated as 'fully active'. Per founder ruling U10: collapse to single 'active'. Today any of the three lets a partner reach the live dashboard, which is functionally OK but creates two ambiguous statuses with no semantic distinction in admin tooling.",
  "why_it_matters": "Footgun: an admin in the queue may set 'verified' thinking it's a tier below 'active' (because the word implies vetting); semantically it is the same. Subtle bugs accumulate as code paths assume one or another. Migration is a 1-line UPDATE.",
  "recommended_fix": "Migration: `UPDATE partners SET status='active' WHERE status IN ('verified','approved'); ALTER TABLE partners ADD CONSTRAINT partners_status_chk CHECK (status IN ('pending','in_review','active','rejected','suspended'));` Refactor app/partner/page.tsx:23 to `const PARTNER_ACTIVE_STATUSES = new Set(['active'])` (or remove the Set entirely and inline `partnerStatus === 'active'`). Sweep all callers of the constant.",
  "blast_radius": "Low — DB migration + 1 file.",
  "implementation_complexity": "Low",
  "test_required": [
    "Migration runs idempotently",
    "INSERT partners with status='verified' fails CHECK",
    "Pre-existing 'verified' / 'approved' rows are migrated to 'active'",
    "/partner dashboard renders for status='active' only"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-015",
  "domain": "auth-onboarding",
  "severity": "P1",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§7 M18 + whitepaper §19: linking_codes table required (6-digit, 24h TTL, single-use) for parent-issued teen invitation codes. Today UI copy says 'code unique de liaison' but backend uses email + 7-day token only.",
  "code_files": [
    "app/api/auth/register-teen/route.ts:74-75",
    "app/api/auth/register-teen/route.ts:117-119"
  ],
  "current_behavior": "Token is `crypto.randomBytes(32).toString('hex')` (64-char hex) with 7-day TTL — not a 6-digit human-readable code, not 24h. The validation URL contains the long token. There is no linking_codes table per Grep (no app file matches). Per canon U13 ruling: 24h for parent-generated linking codes (whitepaper invariant); 7-day token only for the parent-validation EMAIL flow — these are TWO separate mechanisms but only one (the long token) exists.",
  "why_it_matters": "(a) UX: parent cannot read aloud / SMS / write a 6-digit code to a teen offline. (b) Whitepaper invariant violated. (c) The URL-token-only flow means a teen with a phone but no email cannot complete linking through any channel except clicking a link in their parent's email — fragile, doesn't match the spoken UX of 'give your teen this code'.",
  "recommended_fix": "Migration: create linking_codes(code TEXT(6) PRIMARY KEY, parent_id UUID, teen_first_name, teen_last_name, expires_at TIMESTAMPTZ, used_at TIMESTAMPTZ, created_at). Build POST /api/parent/teens/invite that returns {code, expires_at}. Build /auth/link-code page where teen enters the 6-digit code → finds row → triggers admin createUser flow per CANON-AUTH-001. Keep the email-token long-form flow for the asynchronous email-validation path. They co-exist.",
  "blast_radius": "Medium — new table + new API + new public page.",
  "implementation_complexity": "Medium",
  "test_required": [
    "POST /api/parent/teens/invite returns 6-digit code + 24h expiry",
    "Code is single-use (used_at set on consume)",
    "Expired code returns 410",
    "Invalid format (5 digits, alpha) returns 400",
    "Teen flow: enter code → set password → auth.users + profiles row created with profiles.id == auth.users.id"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-016",
  "domain": "auth-onboarding",
  "severity": "P1",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§7 M15: app/join route to capture ?ref=CODE cookie for ambassador attribution MISSING. Per ambassador-referral.md §3.1 every shared ambassador link is currently a 404.",
  "code_files": [
    "app/join/"
  ],
  "code_files_note": "directory does not exist",
  "current_behavior": "No app/join/page.tsx. Ambassador share kits will produce links of shape `https://nivy.app/join?ref=ABC123` that 404 today. Without attribution capture before signup, ambassador commission cannot be paid.",
  "why_it_matters": "Money-pipeline blocker for ambassador track. Without /join the entire ambassador funnel is fictional from the user's POV — they share links that 404 → no signups attributed → no commission paid → ambassadors churn.",
  "recommended_fix": "Build app/join/page.tsx (or app/join/route.ts → 302). Read ?ref=, validate against referral_codes table (must be active), set HttpOnly cookie `nivy_ref=<code>; max-age=2592000` (30d window), then 302 to /. handle_new_user trigger reads cookie/raw_user_meta_data.referrer_code and writes referral_attribution row.",
  "blast_radius": "Low — single route + cookie + trigger update.",
  "implementation_complexity": "Low",
  "test_required": [
    "/join?ref=VALID302 → 302 to / with cookie nivy_ref set",
    "/join with no ref → 302 to / no cookie",
    "/join?ref=INVALID → 302 to / no cookie + log warn",
    "Signup after /join attribution → referral_attribution row written, ambassador commission tracked"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-017",
  "domain": "auth-onboarding",
  "severity": "P1",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§7 M16 + §5 DEPRECATED: '/auth/confirm-email Renvoyer cosmetic button → Wire to POST /api/auth/resend-confirmation or remove'.",
  "code_files": [
    "app/auth/confirm-email/page.tsx",
    "app/api/auth/"
  ],
  "current_behavior": "No app/api/auth/resend-confirmation route exists (Grep returns only canon doc). The 'Renvoyer' button on /auth/confirm-email is therefore cosmetic. Users with a non-arrived confirmation email have no recourse short of contacting support.",
  "why_it_matters": "Email-confirmation hard-required per U8 ruling for parent / partner / mentor / driver / ambassador (money + minor-safety roles). A non-functional resend strands users.",
  "recommended_fix": "Build POST /api/auth/resend-confirmation: rate-limited (1/min/email), accept {email}, call supabase.auth.resend({type:'signup', email}). Wire confirm-email page button to it. Surface success/failure with sonner toast.",
  "blast_radius": "Low — single route + 1 click handler.",
  "implementation_complexity": "Low",
  "test_required": [
    "POST /api/auth/resend-confirmation 429s after >1 call/min/email",
    "Valid email gets new confirmation link",
    "Invalid email returns 200 (no enumeration)",
    "Already-confirmed email returns 200 with no email sent"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-018",
  "domain": "auth-onboarding",
  "severity": "P1",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§4 + §1 teen row + invariant §4.1: teens row required, parent_teen_links row required, teens.id FK to auth.users.id, teen_full_profile is NOT the canonical teen attribute table.",
  "code_files": [
    "app/api/auth/validate-teen/route.ts:206-223",
    "lib/auth/get-user-role.ts:79-83"
  ],
  "current_behavior": "validate-teen approval branch INSERTs into `teen_full_profile` (line 207) instead of the canonical `teens` table per canon §1 / whitepaper §20. lib/auth/get-user-role.ts:80 reads from teen_full_profile (likely a view, but the validate-teen INSERT treats it as a base table). Canon mandates teens.id == auth.users.id == profiles.id; we cannot validate this invariant if the wrong table is being written.",
  "why_it_matters": "Either teen_full_profile is a view (in which case INSERT fails or only works for specific column subsets — no error handling here) or it's a parallel duplicate of teens (in which case canon's per-role attribute table FK to auth.users.id is broken and there are two sources of truth for teen state).",
  "recommended_fix": "Audit live DB: is teen_full_profile a VIEW over teens + … joins, or a base table? If view: replace INSERT with INSERT INTO teens (...). If base table: migrate it to a view over teens + teen_interests + teen_goals + user_xp etc., write INSERTs to teens. Confirm teens.id FK to auth.users.id ON DELETE CASCADE.",
  "blast_radius": "Medium — schema clarification + INSERT path rewrite.",
  "implementation_complexity": "Medium",
  "test_required": [
    "teen_full_profile is a view (DDL inspection)",
    "INSERT INTO teens after auth.admin.createUser yields teen_full_profile row visible to lib/auth/get-user-role.ts",
    "DELETE FROM auth.users CASCADE removes teens row (FK cascade test)"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-019",
  "domain": "auth-onboarding",
  "severity": "P1",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§5 DEPRECATED row 2: '/onboarding for an already-authenticated user → /auth/redirect. The wizard is pre-account only.'",
  "code_files": [
    "app/onboarding/page.tsx",
    "app/auth/redirect/page.tsx:34-37"
  ],
  "current_behavior": "app/onboarding/page.tsx is a client component that does not check auth before rendering — anonymous and authenticated users both see the marketing wizard. /auth/redirect:34 detects 'no profile' and explicitly pushes the user to /onboarding (see CANON-AUTH-003) — the inverse footgun: an authed user with no profile is sent to a wizard that calls auth.signUp (CANON-AUTH-005). Even authed users WITH profiles can navigate manually to /onboarding and re-trigger the wizard.",
  "why_it_matters": "Re-entrancy: an authed parent with a half-onboarded state can trigger a fresh auth.signUp inside the wizard (because parent-setup-step.tsx calls it unconditionally), creating a duplicate auth.users row and a 422 error.",
  "recommended_fix": "Add a server-component guard at app/onboarding/page.tsx top: if (await getUser()) redirect('/auth/redirect'). Combine with the ParentSetupStep deletion of auth.signUp (CANON-AUTH-005). Update /auth/redirect 'no profile' branch to /auth/error?reason=missing_profile (or trigger handle_new_user retry) instead of /onboarding.",
  "blast_radius": "Low — single page guard.",
  "implementation_complexity": "Low",
  "test_required": [
    "Authed visit to /onboarding → 302 to /auth/redirect",
    "Anonymous visit to /onboarding → renders showcase",
    "Authed user with no profile → /auth/error?reason=missing_profile (NOT /onboarding)"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-020",
  "domain": "auth-onboarding",
  "severity": "P1",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§7 M12 + §1 'ambassador' row: /onboarding/ambassador (welcome tour: referral code reveal, share-kit walk-through, payout method) MISSING. Dashboard ships cold-start.",
  "code_files": [
    "app/onboarding/ambassador/"
  ],
  "current_behavior": "No app/onboarding/ambassador/ directory. Approved ambassadors land on /ambassador with no welcome tour, no payout method capture, no referral_codes row reveal. is_onboarded for ambassador role can never reach true (no /api/ambassador/onboarding/complete either).",
  "why_it_matters": "Ambassadors don't know their referral code on first login → cannot share → no commission. Payout method (mobile_money / bank) is never captured → admin cannot process withdrawals → ambassadors abandon the program.",
  "recommended_fix": "Build /onboarding/ambassador/{welcome,referral-code,share-kit,payout-method} chain. Build POST /api/ambassador/onboarding/complete (sets is_onboarded=true, requires referral_codes row + payout_method NOT NULL).",
  "blast_radius": "Low — net-new but isolated.",
  "implementation_complexity": "Medium",
  "test_required": [
    "Approved ambassador → /auth/redirect → /onboarding/ambassador (because is_onboarded=false)",
    "POST /api/ambassador/onboarding/complete fails 412 if payout_method NULL",
    "Successful complete sets is_onboarded=true, redirects to /ambassador"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-021",
  "domain": "auth-onboarding",
  "severity": "P2",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§5 DEPRECATED rows 6-7: ambassador sidebar links pointing at /ambassador/{shop,link,stats,rewards,settings,help,profile} are 404; /ambassador/shop should redirect to /ambassador/boutique.",
  "code_files": [
    "app/ambassador/",
    "components/dashboard/ambassador/"
  ],
  "current_behavior": "Per audit B5 §1.3-1.4, sidebar links to /ambassador/shop|link|stats|rewards|settings|help|profile are 404s; canonical routes are /ambassador/marketing|withdrawals|comment-gagner|boutique. Not directly verified at file level here but referenced in canon §5.",
  "why_it_matters": "Cosmetic but UX-breaking: every sidebar nav click is a 404. Reflects badly on the brand the moment a user reaches the dashboard.",
  "recommended_fix": "Sweep components/dashboard/ambassador/sidebar.tsx (or equivalent) to point at canonical routes. Add a 308 redirect from /ambassador/shop → /ambassador/boutique in next.config.mjs.",
  "blast_radius": "Low — link cleanup.",
  "implementation_complexity": "Low",
  "test_required": [
    "Ambassador sidebar produces no 404 on any link",
    "/ambassador/shop returns 308 to /ambassador/boutique"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-022",
  "domain": "auth-onboarding",
  "severity": "P2",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§6 FORBIDDEN #10: 'No middleware-bypassed dashboard. app/<role>/page.tsx MUST call getUserRole() server-side AND middleware MUST gate the /<role> prefix; both layers required.'",
  "code_files": [
    "middleware.ts:222-230",
    "middleware.ts:280-294"
  ],
  "current_behavior": "Middleware protectedPaths includes /parent, /teen, /ambassador, /partner — but NOT /mentor, /driver, /admin (admin is gated separately at line 185-219, OK). Mentor and driver routes have no middleware gate today; they rely solely on layout-level checks. Per canon §6 #10, both layers are required — single point of failure today for mentor/driver dashboards (once they exist per CANON-AUTH-007/008).",
  "why_it_matters": "When the mentor + driver surfaces ship, a forgotten layout check would expose the dashboard to unauthorised users. Defence in depth requires both.",
  "recommended_fix": "Add /mentor and /driver to protectedPaths. Add /mentor and /driver to dashboardPaths. Update roleRouteMap to include mentor → /mentor/dashboard and driver → /driver/dashboard. Coordinate with §3 redirect rewrite (CANON-AUTH-003).",
  "blast_radius": "Low — middleware constants.",
  "implementation_complexity": "Low",
  "test_required": [
    "Anonymous /mentor/dashboard → 302 /auth/login",
    "Parent at /mentor/dashboard → 302 /auth/redirect → /parent",
    "Anonymous /driver/dashboard → 302 /auth/login"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-023",
  "domain": "auth-onboarding",
  "severity": "P2",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§8 U3 founder ruling + INDEX F3 + §7 M20: 'Influencer = ambassador. Drop /devenir-influenceur candidature, keep /devenir-ambassadeur.' Until decided, /devenir-influenceur MUST NOT grant a role.",
  "code_files": [
    "app/devenir-influenceur/candidature/page.tsx",
    "app/devenir-influenceur/page.tsx"
  ],
  "current_behavior": "/devenir-influenceur and /devenir-influenceur/candidature exist as live pages. Per Grep, code writes to influencer_campaigns table, not ambassadors. There is no influencer role in the canon enum, no influencer dashboard. The page sits in production reachable by users.",
  "why_it_matters": "Confusion + double funnel + dead-end UX. Per F3 ruling: fold into ambassador track via tier='influencer' (audience-size-based) on ambassadors table. Today users who fill the influencer form land in influencer_campaigns and never receive a status update because no admin queue reads that table.",
  "recommended_fix": "Two options: (a) merge — refactor /devenir-influenceur/candidature to write to ambassadors with track='influencer' (after migration adds the column) and 308 /devenir-influenceur → /devenir-ambassadeur (or keep as a marketing landing that links to /devenir-ambassadeur/candidature). (b) delete /devenir-influenceur entirely. Recommend (a) for SEO continuity. Drop influencer_campaigns table (deprecation migration).",
  "blast_radius": "Medium — folded role + table deprecation.",
  "implementation_complexity": "Medium",
  "test_required": [
    "/devenir-influenceur/candidature submits to ambassadors with track='influencer'",
    "ambassadors.track CHECK constraint includes ('organic','influencer')",
    "influencer_campaigns table 410'd (deprecated)",
    "/devenir-influenceur → /devenir-ambassadeur 308"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-024",
  "domain": "auth-onboarding",
  "severity": "P2",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§4 partner Wave 2 wizard: /onboarding/partner/{kyc,first-offer,scanner} MISSING. §7 M9.",
  "code_files": [
    "app/onboarding/partner/"
  ],
  "current_behavior": "Glob app/onboarding/partner/** returns nothing. After admin approval the partner has no post-approval wizard — they land on /partner/dashboard with no first-offer guidance, no scanner setup, no KYC upload UI (Wave 2 KYC documents).",
  "why_it_matters": "Approved partners cannot ship a first offer or wire the scanner without manual hand-holding. Slows time-to-first-redemption. KYC docs (RC, ICE, RIB, CIN of representative) are required for legal compliance per partner-network.md §3 but no upload surface exists.",
  "recommended_fix": "Build the wizard chain. POST /api/partner/onboarding/complete sets is_onboarded=true, requires kyc_documents row(s) approved + ≥1 partner_offers row.",
  "blast_radius": "Medium — wizard + KYC upload + private bucket.",
  "implementation_complexity": "Medium",
  "test_required": [
    "Approved partner with is_onboarded=false → /onboarding/partner/kyc",
    "Cannot reach /partner/dashboard until POST /api/partner/onboarding/complete succeeds",
    "First-offer step requires ≥1 partner_offers row",
    "KYC documents persisted to private bucket with signed URLs only"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-025",
  "domain": "auth-onboarding",
  "severity": "P2",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§7 M17: Magic-link / set-password page for newly-validated teen and newly-registered partner / mentor / driver MISSING. Derived from §1 'partner' / 'teen' / 'mentor' / 'driver' rows.",
  "code_files": [
    "app/auth/set-password/",
    "app/auth/magic-link/"
  ],
  "current_behavior": "No /auth/set-password or /auth/magic-link page exists. Even after CANON-AUTH-001/002/007/008 land and the routes call supabase.auth.admin.generateLink, there's no dedicated landing page. The link emitted by Supabase will route to /auth/callback then /auth/redirect, but the user has no password yet (email-confirmed via admin) — they must be presented with a 'set your password' form before reaching their dashboard.",
  "why_it_matters": "Without a set-password landing the magic-link flow is incomplete: the user is logged in passwordless but cannot log out + back in later. Critical for partner/mentor/driver who will return to their dashboard from different devices.",
  "recommended_fix": "Build /auth/set-password page. After admin createUser flows, generateLink type='recovery' (password setup style) → email lands at /auth/set-password?token=… → user types new password → updateUser → router.push('/auth/redirect').",
  "blast_radius": "Low — single page.",
  "implementation_complexity": "Low",
  "test_required": [
    "Magic-link from validate-teen lands at /auth/set-password",
    "Setting password updates auth.users.encrypted_password",
    "After set, /auth/redirect routes to teen / partner / mentor / driver dashboard correctly"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-026",
  "domain": "auth-onboarding",
  "severity": "P3",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§3 LOCKED + §6 FORBIDDEN #13: 'No fallback redirect to /onboarding for an unknown authed role. Use /auth/error?reason=unknown_role so the bug is observable.'",
  "code_files": [
    "app/auth/redirect/page.tsx:60-62"
  ],
  "current_behavior": "default branch in switch is `router.push('/onboarding')` — silently swallows the unknown-role bug. No /auth/error page exists at app/auth/error/page.tsx (Glob did show one — verify it accepts ?reason=).",
  "why_it_matters": "Telemetry hole. An unknown role today is silently corrected to a marketing wizard that calls auth.signUp again — guarantees a confused user but no error logs.",
  "recommended_fix": "Replace default branch with `router.replace('/auth/error?reason=unknown_role&role=' + encodeURIComponent(profile.role))`. Confirm app/auth/error/page.tsx renders the reason copy. Subsumed by CANON-AUTH-003 fix.",
  "blast_radius": "Low.",
  "implementation_complexity": "Low",
  "test_required": [
    "Authed user with profiles.role='influencer' (post-deprecation) → /auth/error?reason=unknown_role",
    "Sentry / log captures unknown role event"
  ],
  "status": "open"
}
```

```json
{
  "id": "CANON-AUTH-027",
  "domain": "auth-onboarding",
  "severity": "P3",
  "canon_file": "docs/canon/auth-onboarding.locked.md",
  "canon_rule": "§5 DEPRECATED row 6 + 8: 'Direct insert into profiles from app/api/auth/validate-teen / app/api/partners/register without auth.users → Replace with supabase.auth.admin.createUser first.' Plus CI rule (file checklist #13-14): forbid auth.signUp outside app/auth/sign-up/page.tsx + forbid INSERT INTO public.profiles outside handle_new_user trigger.",
  "code_files": [
    ".eslintrc",
    ".github/workflows/"
  ],
  "current_behavior": "No CI rule enforces the FORBIDDEN list. Future PRs can re-introduce the same bugs covered by CANON-AUTH-001/002/005/012.",
  "why_it_matters": "Bug-prevention infrastructure. Without it, regressions are inevitable as new contributors add features.",
  "recommended_fix": "Add ESLint custom rule (or grep-based pre-commit hook) banning: (a) regex `\\.from\\(['\"]profiles['\"]\\)\\.insert` in app/** outside dedicated admin tooling files; (b) `auth\\.signUp` outside app/auth/sign-up/page.tsx; (c) raw SQL `INSERT INTO public.profiles` outside migrations and the handle_new_user function. Wire into pre-commit + CI.",
  "blast_radius": "Low — tooling.",
  "implementation_complexity": "Low",
  "test_required": [
    "PR introducing supabase.from('profiles').insert() in app/api/foo fails CI",
    "PR introducing auth.signUp in components/onboarding/foo.tsx fails CI",
    "Migrations and handle_new_user trigger are exempt"
  ],
  "status": "open"
}
```
