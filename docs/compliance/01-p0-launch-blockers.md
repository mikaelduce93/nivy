# P0 launch blockers — ranked

Generated 2026-05-08. Aggregates every P0 across the 11 domain audits (03–13) plus founder dependencies from 17.

## Stats
- **Total P0 findings**: 56 (12 auth + 11 economy + 4 routing-BLOCKER + 7 social + 9 gamification + 5 parent + 7 partner + 2 lifestyle + 2 AI + 4 admin + 1 design-system, dedup'd to 56 distinct IDs)
- **Domains affected**: 11 / 11
- **Estimated total work**: ~52 engineer-days (sum of complexity sizing)
- **Hard blockers (cannot ship without fix)**: 24
- **Soft P0s (mitigatable via feature flag, redirect, or 410)**: 32

## Ranking method
`Score = (user_impact × 3) + (money/auth/safety × 3) + (blast_radius × 2) + (fix_complexity_inverse)`

Money/auth/safety includes: PSP signature bypass, orphan auth users, public CIN URL, mint-on-demand top-up, race-prone XP debit, distress-signal silent fail.

---

## Top 25 P0s (sorted)

### #1 — CANON-AUTH-001
- **Domain**: auth-onboarding
- **Canon**: docs/canon/auth-onboarding.locked.md §1 + §6 FORBIDDEN #1/#5/#6
- **Current behavior**: `app/api/auth/validate-teen/route.ts:184-196` inserts a `profiles` row with autogenerated UUID and `teen_${id}@teensparty.local` placeholder email. No `supabase.auth.admin.createUser` call. Teen has no `auth.users` row, no credentials, cannot log in. Parent sees fake-success toast.
- **Why it's #1**: every parent-invited teen on the canonical onboarding rail is created as a phantom account. No login = no usage = entire teen graph fictional.
- **Files**: `app/api/auth/validate-teen/route.ts:184-196,206-223,265-271`
- **Minimal fix**: rewrite approval branch to (1) `supabase.auth.admin.createUser({ email, email_confirm:true, user_metadata:{role:'teen', full_name} })`, (2) let `handle_new_user` trigger create profiles row, (3) INSERT teens (id=newUid), (4) INSERT parent_teen_links, (5) `auth.admin.generateLink(magiclink)` and email it, (6) profiles.is_onboarded=false explicit.
- **Test plan**: auth.users row exists; profiles.id == auth.users.id == teens.id; magic-link delivered; request without teen_email returns 400; failed mid-flow rolls back auth.users.
- **Dependencies**: blocked by F1 (self-signup vs parent-invited) and F23 (email confirmation enforcement); blocks CANON-GAME-002 (validate-teen XP grant) and CANON-AUTH-006 (is_onboarded gate).
- **Hard blocker?**: YES
- **Days to fix**: 1.5

### #2 — CANON-ECON-001 / CANON-PARENT-001
- **Domain**: economy-payments + parent-control
- **Canon**: docs/canon/economy-payments.locked.md §6 FORBIDDEN #3/#5; parent-control §2/§9/§10.7
- **Current behavior**: `components/parent/topup-form.tsx:53-64` posts `{parentId, teenId, packageId, coins, bonus, price}` — none is `amount_dh`. Server reads only `body.amount_dh|amountDh`, gets NaN, returns 400. Production top-up 100% broken on `/parent/topup`. If patched to read body.price, mint-on-demand vulnerability.
- **Why it's #2**: money pipeline is dead end-to-end + structurally unsafe.
- **Files**: `components/parent/topup-form.tsx:53-64`, `app/parent/topup/page.tsx:121-162`, `app/api/parent/topup/route.ts:26-50`
- **Minimal fix**: ship `topup_packages` table (id, name, amount_dh, bonus_coins, is_active, sort_order); change form payload to `{teenId, packageId}` only; route looks up package server-side, derives amount_dh; require PSP confirmation (Stripe session.id paid OR Cash Plus webhook ref) before calling top_up_teen; revert to 5-arg top_up_teen requiring p_psp_provider/p_psp_reference.
- **Test plan**: POST `{teenId, packageId}` succeeds with server-derived amount; raw `{coins, price}` rejected; inactive package rejected; PSP confirmation gate blocks credit if reference missing; idempotent on replay.
- **Dependencies**: blocked by F5 (auto-topup launch policy) and F25 (e-money license partnership); pairs with CANON-ECON-009 (Stripe top-up rail) and CANON-PARENT-002 (idempotency).
- **Hard blocker?**: YES
- **Days to fix**: 1

### #3 — CANON-ECON-011 (CMI signature bypass)
- **Domain**: economy-payments
- **Canon**: §4.1 idempotency + §6 FORBIDDEN #3
- **Current behavior**: `lib/payments/cmi.ts:144-156` and `app/api/payments/cmi/webhook/route.ts:11-50` validate HASH only `if (params.HASH)`. POST without HASH is accepted as valid → `payment_transactions(status='completed')` written. Forge a booking_reference, mark it paid.
- **Why it's #3**: dominant Moroccan PSP rail accepts unsigned POSTs as success. Combined with CANON-ECON-009 absent escrow chain = mint paid bookings without money moving.
- **Files**: `lib/payments/cmi.ts:144-156`, `app/api/payments/cmi/webhook/route.ts:11-50`
- **Minimal fix**: at top of `parseCallback`, `if (!params.HASH) return {success:false, code:'HASH_MISSING'}`. Webhook route returns 200 + log without writing any DB row on missing/bad HASH.
- **Test plan**: unsigned POST → no DB write; bad HASH → no DB write; good HASH (signed in test) → exactly one payment_transactions row.
- **Dependencies**: blocks CMI launch entirely; pairs with CANON-ECON-009.
- **Hard blocker?**: YES
- **Days to fix**: 0.25

### #4 — CANON-PARENT-004 (CIN public URL)
- **Domain**: parent-control
- **Canon**: §6 + §10.5; INDEX cross-cut #6
- **Current behavior**: `app/api/parent/e-signature/create/route.ts:73-90,135-155` uploads CIN images to PUBLIC `documents` bucket and stores `getPublicUrl(...)` permanently. Every CIN uploaded today has a permanent public URL. Loi 09-08 / CNDP violation.
- **Why it's #4**: data-protection regulator exposure on minor-protection consent surface.
- **Files**: `app/api/parent/e-signature/create/route.ts:73-90,135-155`; missing `parent-cin` private-bucket migration
- **Minimal fix**: ship `parent-cin` private bucket migration; rewrite upload to use it; replace `getPublicUrl` with `createSignedUrl(path, 5*60)` per F14 TTL ladder; one-shot purge of existing public URLs from `documents.cin_*_url` columns.
- **Test plan**: new CIN uploads land in private bucket; signed URL expires per F14 TTL; old public-URL rows nullified or migrated; admin signed URL has 15-min TTL; bucket policy refuses anon read.
- **Dependencies**: blocked by F14 (TTL ratification).
- **Hard blocker?**: YES
- **Days to fix**: 1

### #5 — CANON-PARENT-005
- **Domain**: parent-control
- **Canon**: §7 + INDEX cross-cut #2
- **Current behavior**: `app/api/parent/teens/create/route.ts:142-194` inserts `profiles {role:'teen'}` + `parent_teen_links` directly. NO `supabase.auth.admin.createUser`. Created teen cannot log in.
- **Why it's #5**: second teen-creation path with same orphan-profile bug as CANON-AUTH-001.
- **Files**: `app/api/parent/teens/create/route.ts:142-194,197,207`
- **Minimal fix**: identical pattern to CANON-AUTH-001 — auth.admin.createUser → trigger creates profile → INSERT teens → INSERT parent_teen_links → magic-link email. Replace deprecated `notifications`/`activity_logs` writes with `user_notifications`.
- **Test plan**: created teen has auth.users row; FK invariants hold; magic-link delivered; deprecated table writes gone.
- **Dependencies**: blocked by F1; same fix template as CANON-AUTH-001.
- **Hard blocker?**: YES
- **Days to fix**: 1

### #6 — CANON-AUTH-002 / CANON-PARTNER-001 / CANON-PARTNER-002
- **Domain**: auth-onboarding + partner-ecosystem
- **Canon**: auth §1 partner row + §7 M10; partner-ecosystem §2 + §4.7 + F1
- **Current behavior**: `app/api/partners/register/route.ts:29-50` inserts `partners` row only. No auth.users, no profiles, no magic-link. `app/api/admin/partners/[id]/activate` does NOT exist; `approve` only flips status. Partner cannot log in even after admin approval.
- **Why it's #6**: entire partner ecosystem unreachable; admin approval is theatrical.
- **Files**: `app/api/partners/register/route.ts:29-50,78-85`; missing `app/api/admin/partners/[id]/activate/route.ts`; `app/api/admin/partners/[id]/approve/route.ts:62-66`
- **Minimal fix**: build `register_partner` SECURITY DEFINER RPC for atomic auth.users + profiles + partners insert + transient `partner_pending_credentials` (per F21). Build `POST /api/admin/partners/[id]/activate` with the 6-step transaction (auth.admin.createUser, profiles trigger, partner_staff role='owner', invite-link, status='active'). `approve` route delegates to it.
- **Test plan**: register → auth.users + profiles + partners(status=pending); approve → activate transaction completes; partner can log in; KYC fields persist; mid-failure rolls back auth.users.
- **Dependencies**: blocked by F21 (partner auto-create at apply); blocks /partner KYC flow.
- **Hard blocker?**: YES
- **Days to fix**: 2

### #7 — CANON-AUTH-004 (role enum migration)
- **Domain**: auth-onboarding (cross-cut INDEX #1)
- **Canon**: §2 LOCKED 7-value enum
- **Current behavior**: `profiles.role` is plain TEXT with no CHECK constraint. Live data has only `parent | partner | teen`. Code branches on `mentor | driver | ambassador | admin` are dead. UserRole TS union conflates admin sub-roles into profile role.
- **Why it's #7**: blocks every other role-based fix (CANON-AUTH-003 redirect, CANON-AUTH-007 mentor, CANON-AUTH-008 driver, CANON-PARTNER-020 driver enum).
- **Files**: `gamification-system/database/all_migrations.sql:8`, `lib/auth/get-user-role.ts:3`, `lib/auth/admin-permissions.ts:99`
- **Minimal fix**: migration `ALTER TABLE profiles ADD CONSTRAINT profiles_role_chk CHECK (role IN ('parent','teen','partner','mentor','driver','ambassador','admin'))`; refactor admin-permissions.ts to read sub-role from `admin_roles.role`; refactor UserRole TS union; backfill any stray 'super_admin'/'moderator'/'support' values to role='admin' + admin_roles.role.
- **Test plan**: migration idempotent; INSERT with 'influencer' fails CHECK; INSERT with 'mentor' succeeds; admin queries continue working.
- **Dependencies**: blocked by F2 (driver role) and F3 (influencer fold); pre-req for AUTH-003/007/008/PARTNER-020.
- **Hard blocker?**: YES
- **Days to fix**: 0.5

### #8 — CANON-AUTH-003 (redirect switch)
- **Domain**: auth-onboarding
- **Canon**: §3 LOCKED redirect table
- **Current behavior**: `app/auth/redirect/page.tsx:43-62` switch handles teen/parent/ambassador/partner/admin only. Mentor + driver hit default → `/onboarding` (the marketing wizard with forbidden auth.signUp). No status-aware sub-routing. No is_onboarded gating.
- **Why it's #8**: the post-login choke point is broken for 2 roles + ignores onboarding state.
- **Files**: `app/auth/redirect/page.tsx:43-62`
- **Minimal fix**: replace switch with full §3 LOCKED truth table; read partners.status, mentors.kyc_status, nivy_drivers.kyc_status, ambassadors.status, profiles.is_onboarded; default branch `/auth/error?reason=unknown_role`; convert to server-side route handler.
- **Test plan**: 8 redirect cases per canon truth table all pass.
- **Dependencies**: blocked by CANON-AUTH-004 (enum); pairs with CANON-AUTH-006 (is_onboarded gate).
- **Hard blocker?**: YES
- **Days to fix**: 1

### #9 — CANON-PARENT-003 / CANON-PARENT-025 (approvals cascade)
- **Domain**: parent-control
- **Canon**: §5 routing + §10.1 FORBIDDEN
- **Current behavior**: `app/api/parent/approvals/route.ts:63-136` flips `parental_approvals.status` and inserts a teen notification — never calls `parent_approve_session/_ride/_purchase/_food/_content`. Response includes a `hint` field telling engineers to call cascade. 4 of 5 cascade RPCs are MISSING in SQL.
- **Why it's #9**: every parental approval is a UI lie. Coins not debited, ride not booked, food not placed.
- **Files**: `app/api/parent/approvals/route.ts:63-136`; missing RPCs: `parent_approve_ride`, `parent_approve_purchase`, `parent_approve_food`, `parent_approve_content`
- **Minimal fix**: build the 4 missing RPCs in canonical form; rewrite approvals route to dispatch by `action_type` to the correct RPC; remove the `hint` shipping convention.
- **Test plan**: approve session → mentor session row updated; approve ride → ride dispatched; approve purchase → coins debited; approve food → order placed; approve content → flag flipped.
- **Dependencies**: pairs with CANON-PARENT-016 (action_type column); blocks any meaningful parent-control launch.
- **Hard blocker?**: YES
- **Days to fix**: 2

### #10 — CANON-ECON-004 / CANON-GAME-001/002/003 (add_user_xp phantom)
- **Domain**: economy + gamification + partner
- **Canon**: economy §5.2 + §8; gamification §7/§8/§9; INDEX cross-cut
- **Current behavior**: 3 callsites call phantom RPC `add_user_xp` (`quests/complete:94`, `validate-teen:265-271`, `partner/apply-discount:188-193`). Errors swallowed by try/catch. Quest XP, parent verification XP, partner cashback XP all silently no-op. Optimistic UI lies on every grant.
- **Why it's #10**: highest-impact production bug per audit; trust-eroding silent failure on the core retention loop.
- **Files**: `app/api/teen/quests/complete/route.ts:94`, `app/api/auth/validate-teen/route.ts:265-271`, `app/api/partner/apply-discount/route.ts:188-193`
- **Minimal fix**: rename calls to `add_xp_to_user`, rename `p_user_id`→`p_teen_id`, add `p_source_type`/`p_source_category`/`p_description`. Remove silent catches. Ship SQL alias `CREATE OR REPLACE FUNCTION add_user_xp(...) AS $$ SELECT add_xp_to_user(...) $$` as belt-and-suspenders. Also fix `lib/hooks/teen-dashboard.ts:130-132` `get_user_xp` phantom.
- **Test plan**: quest complete → xp_transactions row + user_xp.total_xp delta; parent verify → +50 XP; partner apply → +floor(amount/10) XP; grep `add_user_xp` returns 0 in app/api.
- **Dependencies**: paired with CANON-AUTH-001 (validate-teen rewrite happens at same time).
- **Hard blocker?**: YES
- **Days to fix**: 0.5

### #11 — CANON-ECON-008 (race-prone XP debit)
- **Domain**: economy-payments
- **Canon**: §6 FORBIDDEN #1 + #2; §29.4
- **Current behavior**: `app/api/payments/hybrid/route.ts:164-186` and `app/api/payments/xp/route.ts:221-243` do read-then-write XP debits directly: SELECT user_xp → UPDATE → INSERT xp_transactions. No FOR UPDATE. Two concurrent payments double-debit; rollback path is itself a non-atomic UPDATE.
- **Why it's #11**: §29 invariant the canon was written to prevent. Race-prone double-spend.
- **Files**: `app/api/payments/hybrid/route.ts:164-186,332-337`, `app/api/payments/xp/route.ts:221-243,265-270`
- **Minimal fix**: build `spend_xp_for_booking(p_teen_id, p_booking_id, p_amount_xp)` SECURITY DEFINER RPC with FOR UPDATE; routes call only the RPC; rollback handled inside the RPC.
- **Test plan**: concurrent paste of 2 hybrid POSTs same booking → exactly one succeeds; PSP failure → no partial debit; xp_transactions row per success.
- **Hard blocker?**: YES
- **Days to fix**: 1

### #12 — CANON-ECON-009 (Stripe + CMI direct money writes)
- **Domain**: economy-payments
- **Canon**: §6 FORBIDDEN #2; §4.1 audit chain
- **Current behavior**: Stripe `handleCoinTopup` writes to non-existent `profiles.coins` column then directly INSERTs `coin_transactions` — no top_up_teen RPC, no escrow_ledger, no payment_transactions row. CMI callback + webhook directly INSERT `payment_transactions`. Admin refund route's `bumpCoins` does direct user_coins UPDATE.
- **Why it's #12**: Stripe top-ups (diaspora rail) skip the entire audit chain. Two parallel ledger conventions.
- **Files**: `app/api/webhooks/stripe/dispatcher.ts:67-86,79`, `app/api/payments/cmi/callback/route.ts:77-89,111-122`, `app/api/payments/cmi/webhook/route.ts:80-93`, `app/api/admin/refunds/route.ts:185-200,269-277,347-356,428-437`
- **Minimal fix**: Stripe coin_topup rewrites to call `top_up_teen(parent, teen, amount_dh, 'stripe', session.id)`. CMI callback + webhook same with provider='cmi'. Build `refund_teen_coins` RPC; admin refunds route through it; delete `bumpCoins`.
- **Test plan**: Stripe topup → coin_transactions + payment_transactions + escrow_ledger linked; CMI same; admin refund inserts escrow_ledger(direction='refund'); duplicate Stripe event is no-op.
- **Dependencies**: pairs with CANON-ECON-015 (refund_teen_coins build); blocks CANON-LIFE-001/002 (admin refund 'refunded' status writes).
- **Hard blocker?**: YES
- **Days to fix**: 2

### #13 — CANON-ECON-007 (cancel_ride no-op refund)
- **Domain**: economy-payments + lifestyle
- **Canon**: §7 + §4.6 + §29.4
- **Current behavior**: `cancel_ride` RPC computes `v_refund_pct` (100 or 50) but has TODO comment and returns. No coin_transactions, no escrow_ledger refund row. Teens lose coins forever on cancellation.
- **Why it's #13**: direct money loss for users on a publicly-offered cancellation feature.
- **Files**: `gamification-system/database/migrations/057_transport_mobility_rpcs.sql:107-117`, `app/api/teen/rides/[id]/cancel/route.ts:23-30`
- **Minimal fix**: build `refund_teen_coins(p_teen_id, p_amount_coins, p_source_type, p_source_id, p_reason)` with FOR UPDATE + escrow_ledger(direction='refund'); wire into cancel_ride after status flip; reverse cashback via `revoke_xp_cashback`.
- **Test plan**: cancel >60min before → 100% refund; <60min → 50%; idempotent on replay.
- **Dependencies**: required by CANON-ECON-009 + CANON-LIFE-001/002.
- **Hard blocker?**: YES
- **Days to fix**: 0.75

### #14 — CANON-AI-001 (PII to LLM)
- **Domain**: personalization-ai
- **Canon**: §11 #2 (pseudonym + age-bucket only)
- **Current behavior**: `lib/ai/context-engine.ts:81-95,152-157` SELECTs `full_name`, `first_name`, `city`, `avatar_url`. `app/api/agent/action/route.ts:148-161` `JSON.stringify(context)` into system prompt — full_name + city land in every OpenAI call. Parent context exfiltrates each linked teen's full_name.
- **Why it's #14**: CNDP/RGPD exposure to US LLM processor on every chat turn for every teen.
- **Files**: `lib/ai/context-engine.ts:81-95,152-157,243-251`, `app/api/agent/action/route.ts:114-118,148-161`; pair with `app/api/teen/avatar-coach/route.ts:263-265,101-124,303,306` (CANON-AI-005)
- **Minimal fix**: strip full_name/first_name from SELECTs; replace with `pseudo` lookup from teens; switch to whitelisted projection `{pseudo, age_bucket, gamification, activeQuests, social.onlineFriendsCount}`; ship CI grep banning PII column reads alongside model provider imports.
- **Test plan**: snapshot test: prompt for teen with full_name='Yassine Benali' contains neither 'Yassine' nor 'Benali'; parent prompt contains no children.full_name.
- **Hard blocker?**: YES (regulatory)
- **Days to fix**: 0.5

### #15 — CANON-AI-002 (retired Claude model literal)
- **Domain**: personalization-ai
- **Canon**: §8 + §11 #1; INDEX cross-cut deprecation
- **Current behavior**: `lib/ai/providers/factory.ts:18` defaults `model || 'claude-3-sonnet-20240229'` (retired snapshot). Line 13 defaults to `'gpt-4'`. `lib/ai/provider.ts:15` hardcodes `'gpt-4o-mini'` instead of `OPENAI_MODEL_ID`. Any caller without explicit model 4xxes silently.
- **Why it's #15**: Wave 1 F4 was claimed done but isn't. Crons silently 404.
- **Files**: `lib/ai/providers/factory.ts:13,18`, `lib/ai/provider.ts:15`
- **Minimal fix**: read `process.env.OPENAI_MODEL_ID` and `process.env.CLAUDE_MODEL_ID` with constants from `lib/ai/content-generator.ts`; CI grep banning literal model IDs across lib/app/components.
- **Test plan**: factory with no model arg uses env; CI grep finds no `claude-3-` / `gpt-4'` / `gpt-4o-mini'` literals.
- **Hard blocker?**: YES
- **Days to fix**: 0.25

### #16 — CANON-SOCIAL-015 (user_reports table missing)
- **Domain**: social-feed + admin-moderation
- **Canon**: social-feed §7 #1; admin §3
- **Current behavior**: every `report` action targets `user_reports` per canon, but the table does not exist (grep returns only docs). Combined with CANON-SOCIAL-005 writing to `reports`, CANON-SOCIAL-013 writing to `moderation_reports`, ALL reports silently fail; auto-hide threshold never triggers.
- **Why it's #16**: minor-protection moderation surface non-functional. Every "Signaler" button is theatrical.
- **Files**: missing migration; consumers: `app/api/teen/feed/comments/route.ts:298-305`, `app/api/circles/report/route.ts:89-128`, feed-list `report` button
- **Minimal fix**: ship migration `user_reports(id, reporter_user_id, target_type, target_id, reason, status, reviewed_by, reviewed_at, created_at, UNIQUE(reporter, target_type, target_id))` + RLS; migrate feed/comments/circle/marketplace/profile reports to write here; threshold count uses same table.
- **Test plan**: report from feed → row inserted; 3rd report flips visibility flag (per CANON-ADMIN-006 trigger); admin can read; reporter cannot self-read others.
- **Dependencies**: pairs with CANON-ADMIN-005 (single moderation inbox) and F7 (single inbox ratification).
- **Hard blocker?**: YES
- **Days to fix**: 0.5

### #17 — CANON-SOCIAL-014 / CANON-ADMIN-001/002 (audit_log canonical)
- **Domain**: admin-moderation + social-feed
- **Canon**: INDEX cross-cut #7; admin §4 + §10.8/9
- **Current behavior**: `lib/auth/admin-permissions.ts:165` writes to `admin_audit_logs` but the table is missing in live DB; canon mandates singular `audit_log` with different shape (`actor_id`, `actor_role`, `target_user_id`). Every privileged write silently fails. 28 producers across app/api use the wrong shape.
- **Why it's #17**: CNDP audit chain inoperative. Compliance-grade evidence of admin actions absent.
- **Files**: `lib/auth/admin-permissions.ts:155-176`; 28 callsites listed in CANON-ADMIN-001
- **Minimal fix**: migration creating canonical `audit_log` (BIGSERIAL pk, actor_id, actor_role, action, resource_type, resource_id, target_user_id, description, metadata, ip, ua, created_at); rewrite logAdminAction with `.throwOnError()`; sweep 28 callsites.
- **Test plan**: every admin mutation writes audit_log row with correct shape; insert failure throws; backfill of any extant admin_audit_logs.
- **Dependencies**: ratifies INDEX #7 over social-feed §7 invariant 4 contradiction.
- **Hard blocker?**: YES
- **Days to fix**: 1.5

### #18 — CANON-PARTNER-003/004 (scanner replayable, no qr_nonces)
- **Domain**: partner-ecosystem
- **Canon**: §4.2 + F2 + F7
- **Current behavior**: `app/api/partner/verify-card/route.ts:47-54` accepts static `TPVIP:userId:cardNumber` — no HMAC, no expiry, no nonce. Any leaked card photo is permanently usable. `qr_nonces` table absent. Apply path uses non-atomic JS-side `current_total_uses + 1` increment.
- **Why it's #18**: replay attack + race-prone counter on partner discount apply.
- **Files**: `app/api/partner/verify-card/route.ts:47-54`, `app/api/partner/apply-discount/route.ts:170-175`; missing `qr_nonces` migration + `apply_partner_offer` RPC
- **Minimal fix**: migrate QR to `nivy:v1:{user_id}:{card_number}:{exp_unix}:{nonce}:{hmac_sha256}`; create `qr_nonces`; build `apply_partner_offer` SECURITY DEFINER RPC with offer-row lock + nonce insert + counter increment + discount_usage write + idempotency_key UNIQUE.
- **Test plan**: v1 valid + signed HMAC scan succeeds; expired exp_unix rejected; replay (same nonce) rejected; concurrent apply with same nonce → exactly one success.
- **Hard blocker?**: YES
- **Days to fix**: 1.5

### #19 — CANON-PARTNER-005 (offer auto-live bypasses moderation)
- **Domain**: partner-ecosystem
- **Canon**: §4.1 + F3 + §3.2 CHECK
- **Current behavior**: `app/api/partner/offers/route.ts:245` writes `is_active: true` directly. No `status` column populated. Partner self-creates a live offer in one POST.
- **Why it's #19**: any partner offer (including illegal/abusive copy) goes live to all teens with no review.
- **Files**: `app/api/partner/offers/route.ts:245`; missing migration adding status enum + CHECK
- **Minimal fix**: default `is_active=false, status='pending_approval'`; migration adding `partner_offers.status` enum + `CHECK (NOT is_active OR status='approved')`; build `POST /api/admin/partners/offers/[id]/decision`.
- **Test plan**: create offer → status='pending_approval', is_active=false; admin approve → flips both; teen feed never sees pending; CHECK rejects is_active=true with status≠approved.
- **Hard blocker?**: YES
- **Days to fix**: 1

### #20 — CANON-PARTNER-009 (no partner KYC upload UI)
- **Domain**: partner-ecosystem
- **Canon**: §7.2 + §2 stage 4
- **Current behavior**: `/partner/kyc` is read-only (file header confirms). No `/devenir-{archetype}/kyc?token=…` flow. No `POST /api/partner/kyc/upload`. Partners cannot deliver legal docs.
- **Why it's #20**: partner cannot satisfy KYC → cannot reach `active` → entire ecosystem blocked.
- **Files**: missing routes; `app/partner/kyc/page.tsx:1-13`
- **Minimal fix**: build signed-link uploader at `/devenir-{archetype}/kyc?token={signed_jwt}`; build `POST /api/partner/kyc/upload` writing to private `kyc-documents` bucket; reuse `kyc_documents` table per CANON-ADMIN-009.
- **Test plan**: signed-link upload writes to private bucket; admin sees pending row; expired token rejected.
- **Dependencies**: pairs with CANON-PARTNER-001/002 + CANON-ADMIN-009.
- **Hard blocker?**: YES
- **Days to fix**: 1.5

### #21 — CANON-LIFE-001/002 (admin refund 'refunded' status not in CHECK)
- **Domain**: lifestyle + admin
- **Canon**: lifestyle §1/§2; §8 #1
- **Current behavior**: `app/api/admin/refunds/route.ts:401-406` writes `'refunded'` to `ride_bookings.status`; line 323-329 writes same to `food_orders.status`. Neither value is in the CHECK constraint. Every admin refund throws at the DB layer; teen never gets coins back.
- **Why it's #21**: 100% breakage on admin refund path.
- **Files**: `app/api/admin/refunds/route.ts:401-406,323-329`
- **Minimal fix**: build `refund_ride` and `refund_food_order` RPCs mirroring `complete_*` shape (clawback coins via `refund_teen_coins`, reverse cashback, write escrow_ledger); admin route calls only RPCs; do NOT add 'refunded' to CHECK (use existing 'cancelled' + refund_id linkage per F46).
- **Test plan**: admin refunds ride → coins back, escrow_ledger row, status='cancelled' + refund_id; food same.
- **Dependencies**: blocked by CANON-ECON-007 (refund_teen_coins).
- **Hard blocker?**: YES
- **Days to fix**: 0.75

### #22 — CANON-ADMIN-007 (no /admin/finances; refund flow split)
- **Domain**: admin-moderation
- **Canon**: §5 + §11.13
- **Current behavior**: `/admin/finances` does not exist; `/admin/topups` is sibling route, not tab. Refunds API does inline coin updates instead of named RPCs (violates §10.5). 6 named RPCs missing (`refund_booking`, `refund_food_order`, `refund_marketplace`, `reverse_topup`, `release_partner_payout`, `mark_partner_payout_failed`). `refunds` table missing. ACL accepts moderator (canon=admin only).
- **Why it's #22**: financial controls absent; 4 of 5 escrow surfaces have no canonical RPC path.
- **Files**: missing `app/admin/finances/`; `app/api/admin/refunds/route.ts:61,280-440`; missing migration
- **Minimal fix**: build the 6 RPCs + `refunds` table + `/admin/finances` tabbed page (Refunds, Top-ups, Payouts, Reconciliation); migrate `/admin/topups` into Top-ups tab; tighten ACL to admin/super_admin; D6 two-person rule on partial refunds ≥ 200 DH.
- **Test plan**: each RPC inserts paired escrow row; admin queue shows pending refunds; moderator role rejected.
- **Dependencies**: pairs with CANON-ECON-009/-015 + CANON-LIFE-001/002.
- **Hard blocker?**: YES
- **Days to fix**: 3

### #23 — CANON-AUTH-006 (no is_onboarded gate)
- **Domain**: auth-onboarding
- **Canon**: §4 LOCKED + §7 M14
- **Current behavior**: `is_onboarded` column may not exist live; only teen has `/api/teen/onboarding/complete`. Middleware never reads it. Parents skip CGU e-signature, top-up, spend-mode. Partners skip KYC, first-offer, scanner. CNDP minor-protection posture is fictional.
- **Why it's #23**: every "wizard before dashboard" promise is decoration.
- **Files**: `middleware.ts:221-308`; missing `/api/{parent,partner,mentor,driver,ambassador}/onboarding/complete` routes
- **Minimal fix**: confirm column exists NOT NULL DEFAULT false; backfill legacy users true; build 5 missing complete routes with hard preconditions (e_signatures.signed_at, kyc_documents.status='approved', etc.); middleware redirects is_onboarded=false to role wizard root; admin bypasses; trigger resets on role change.
- **Test plan**: each role in is_onboarded=false → redirected to wizard; complete endpoint requires precondition; admin bypasses; role change resets.
- **Dependencies**: blocked by CANON-AUTH-003 + F22.
- **Hard blocker?**: YES (CNDP)
- **Days to fix**: 2

### #24 — CANON-AI-006 (distress signal silent)
- **Domain**: personalization-ai
- **Canon**: §9 (parent ping + chat lock + MA hotline)
- **Current behavior**: `app/api/teen/avatar-coach/route.ts:59-78` deny-list catches self-harm tokens, returns canned French text. NO hotline number, NO parent ping (`user_notifications`), NO chat lock. Teen retries up to daily cap.
- **Why it's #24**: minor-safety gap on AI surface for the most consequential intent.
- **Files**: `app/api/teen/avatar-coach/route.ts:59-78`
- **Minimal fix**: branch self-harm subgroup → insert `user_notifications` to linked parent (urgency=high); set `teens.coach_locked_until = NOW() + INTERVAL '1 hour'`; append MA hotline; top-of-route `if (teens.coach_locked_until > NOW()) return 423`.
- **Test plan**: self-harm token triggers all 3 reactions; subsequent call within hour returns 423.
- **Hard blocker?**: YES (safety)
- **Days to fix**: 0.5

### #25 — CANON-ROUTE-002/003/004 (auth nav components broken)
- **Domain**: routing
- **Canon**: §5 rules 1, 3, 4, 5, 8, 13
- **Current behavior**: 3 of 4 cross-cutting nav components (`components/layouts/app-sidebar.tsx`, `components/dashboard/sidebar.tsx`, `components/dashboard/header.tsx`) point at forbidden URLs (`/dashboard`, `/profile`, `/mon-compte`, `/notifications`, `/mes-reservations`). Mobile dock points at 6 routes that don't exist on disk (`/admin/events`, `/admin/users`, `/admin/settings`, `/partner/profile`, `/ambassador/profile`, `/ambassador/shop`).
- **Why it's #25**: 6 guaranteed 404s in dominant nav surface; sidebar links 8+ deprecated/forbidden URLs visible to every authed user.
- **Files**: `components/layouts/app-sidebar.tsx:37,47,52,57,67,73,79`; `components/dashboard/sidebar.tsx:22,28,34,42,48,52,58,62,68`; `components/dashboard/header.tsx:40,41,42,44,45,103,131,137`; `components/layouts/mobile-dock.tsx:145,162,169,183,207,221`
- **Minimal fix**: re-point or delete app-sidebar/dashboard-sidebar (likely delete in favor of role-specific sidebars); rewrite header hrefs to canonical role-prefixed URLs; replace mobile dock targets per canon §1.7 (`/admin/evenements`, `/admin/utilisateurs`, drop `/admin/settings`, `/partner/settings`, drop `/ambassador/profile`, drop `/ambassador/shop`).
- **Test plan**: every nav link resolves to a rendered route; no 404 on click; ESLint rule banning forbidden URLs.
- **Hard blocker?**: YES (every authed user sees broken nav)
- **Days to fix**: 0.75

---

## Remaining P0s (table, summary form)

| ID | Domain | One-line | Days | Hard? |
|---|---|---|---|---|
| CANON-AUTH-005 | auth | Marketing wizard calls auth.signUp (forbidden); reroute to /auth/sign-up?source=wizard | 0.25 | YES |
| CANON-AUTH-007 | auth | /devenir-mentor + candidature + KYC upload + /mentor/onboarding/* MISSING | 4 | YES |
| CANON-AUTH-008 | auth | /devenir-chauffeur + /driver/* + driver/apply MISSING (gated by F2/F42) | 5 | YES |
| CANON-AUTH-009 | auth | Ambassador apply route missing + admin approval not atomic role flip | 1 | YES |
| CANON-AUTH-010 | auth | Parent wizard chain (e-sign/add-teen/topup/spend-mode) MISSING | 3 | YES |
| CANON-AUTH-011 | auth | admin_roles.permissions JSONB column MISSING; permission overrides silently no-op | 0.5 | YES |
| CANON-AUTH-012 | auth | Orphan profiles + missing CI rule banning direct profiles INSERT | 0.5 | NO |
| CANON-ECON-002 | economy | /api/teen/shop legacy rail (deduct_user_xp) — should 410 | 0.25 | NO |
| CANON-ECON-003 | economy | /api/teen/tokens legacy rail (transfer_tokens P2P, exchange) — should 410 | 0.5 | NO |
| CANON-ECON-005 | economy | Wallet route uses wrong PK col (user_id vs teen_id); shows zeros | 0.1 | YES |
| CANON-ECON-006 | economy | shop_purchases.coins_spent is XP — currency mislabel | 0.5 | NO |
| CANON-ECON-010 | economy | Hybrid checkout hardcodes paymentMethod='stripe'; CMI/MM dead | 0.5 | NO |
| CANON-ECON-015 | economy | Missing canonical RPCs (refund_teen_coins, revoke_xp_cashback, refund_top_up, complete_mentor_session, etc.) | 3 | YES |
| CANON-GAME-004 | game | /teen/challenges re-export — convert to redirect | 0.1 | NO |
| CANON-GAME-006 | game | 5 /gamification/* pages still render content (hub/parcours/leaderboard/roue/collections) | 0.5 | NO |
| CANON-GAME-007 | game | Friend-defi decline POSTs to /accept (silently accepts) | 0.1 | YES |
| CANON-GAME-008 | game | Chore evidence uploads to defi-proofs bucket (wrong) | 0.1 | YES |
| CANON-GAME-017 | game | Optimistic XP delta lies (closes when 001/002/003 land) | 0 | NO |
| CANON-GAME-020 | game | validate-teen creates profile without auth.users (= AUTH-001) | 0 | YES |
| CANON-SOCIAL-001 | social | Feed limit(30) hardcoded; canon = cursor 20/page | 1 | NO |
| CANON-SOCIAL-002 | social | Feed list missing per-row liked/saved/reported state | 0.5 | NO |
| CANON-SOCIAL-003 | social | window.alert() as success toast in feed report/block | 0.25 | YES |
| CANON-SOCIAL-004 | social | Comments thread UI absent on /teen/feed/[id] (backend ready) | 1.5 | YES |
| CANON-SOCIAL-007 | social | 5 friend routes missing (unfriend/block/unblock/search/discover) | 1 | YES |
| CANON-SOCIAL-009 | social | DM realtime channel `dm:${conversationId}` not subscribed | 0.5 | YES |
| CANON-SOCIAL-013 | social | Circle reports write to non-existent moderation_reports table | 0.25 | YES |
| CANON-PARENT-002 | parent | Top-up has no client_idempotency_key plumbing | 0.5 | YES |
| CANON-PARTNER-012 | partner | 11 of 15 archetypes have no surface | 8 | NO |
| CANON-LIFE-007 | lifestyle | /driver/** workspace missing (gated by F42) | 3 | NO |
| CANON-ADMIN-005 | admin | Moderation fragmented across 4 routes; canonical /admin/moderation MISSING | 2 | YES |
| CANON-ADMIN-008 | admin | /admin/broadcasts route + broadcasts table MISSING | 1.5 | NO |
| CANON-ADMIN-011 | admin | /admin/scripts-sql exposed without super_admin gate | 0.25 | YES |
| CANON-DS-001 | design-system | NotificationBell missing aria-label + 21 raw palette utilities | 0.25 | NO |

---

## Minimum P0 set to reach RISKY launch (≤ 2 weeks)

The 12 IDs that, if fixed, bring overall score above 40 and clear all hard-blockers:

1. **CANON-AUTH-004** — role enum CHECK migration (0.5d) — unblocks 4 downstream
2. **CANON-AUTH-001 + CANON-PARENT-005** — teen auth.users bootstrap (1.5d) — unblocks 2 phantom-account paths
3. **CANON-AUTH-003** — redirect switch with 7 roles + status-aware (1d)
4. **CANON-AUTH-006** — is_onboarded gate + 5 complete endpoints (2d)
5. **CANON-ECON-001/PARENT-001 + CANON-PARENT-002** — top-up payload + idempotency (1.5d)
6. **CANON-ECON-008** — atomic spend_xp_for_booking RPC (1d)
7. **CANON-ECON-009 + CANON-ECON-015 (refund_teen_coins + revoke_xp_cashback only)** — money write unification (2d)
8. **CANON-ECON-011** — CMI HASH enforcement (0.25d)
9. **CANON-ECON-004 / GAME-001/002/003** — add_xp_to_user rename (0.5d)
10. **CANON-PARENT-003 + CANON-PARENT-025** — approvals cascade RPCs + fan-out (2d)
11. **CANON-PARENT-004** — CIN to private parent-cin bucket (1d)
12. **CANON-AI-001 + CANON-AI-002 + CANON-AI-006** — PII strip + model env + distress reactions (1.25d)

Sub-total: ~14.5 engineer-days. Parallelizable across 6 agents → 2.5 calendar weeks with safety review.

After this set lands:
- AUTH score: 22 → 60
- ECON score: 38 → 70
- PARENT score: 38 → 65
- AI score: 62 → 82
- SOCIAL/PARTNER/ROUTING: still <50 but unblocked structurally
- **Overall composite: ~52** — RISKY but launchable to closed beta on parent-only invites.

Hard-blockers cleared:
- Teen accounts can be created
- Money pipeline works end-to-end with idempotency + escrow
- CMI signature enforced
- CIN private
- Admin approvals cascade
- Phantom XP RPC retired
- AI does not exfiltrate PII
- Distress safety reaction fires

Hard-blockers REMAINING after RISKY set (defer to Wave 3):
- Partner ecosystem (CANON-PARTNER-001/002/003/009) — closed beta runs partner-light
- Driver workspace (gated by F2/F42 ratification)
- /admin/finances + /admin/moderation unification
- Mentor surface
- 5 missing /gamification redirects (mitigatable via 410)

---

## Founder dependencies blocking P0s

| Founder ruling | Required before | If not ratified, P0 fix... |
|---|---|---|
| **F1** (self-signup vs parent-invited) | CANON-AUTH-001 + AUTH-005 | ships parent-invited only as canonical (matches recommendation) |
| **F2** (driver as role) | CANON-AUTH-004 + AUTH-008 + LIFE-007 | enum migration omits driver; driver work deferred to V1.5 |
| **F3** (influencer fold) | CANON-AUTH-023 + PARTNER-012 | inter-canon contradiction unresolved; both pages live |
| **F5** (auto-topup launch) | CANON-ECON-001/PARENT-001 + PARENT-021 | top-up form ships behind PSP_AUTO_TOPUP_ENABLED=false flag (manual rail only) |
| **F7** (single moderation inbox) | CANON-ADMIN-005 + SOCIAL-013/015 | ships single inbox (matches recommendation); blocks if revisited |
| **F14** (CIN TTL ladder) | CANON-PARENT-004 | private bucket ships with conservative 5min TTL (matches recommendation) |
| **F21** (partner auto-create at apply) | CANON-AUTH-002 + PARTNER-001/002 | implementation depth depends on transient credentials table approval |
| **F22** (is_onboarded reset on role change) | CANON-AUTH-006 | trigger ships ON (matches recommendation) |
| **F23** (email confirmation enforcement) | CANON-AUTH-001 + AUTH-017 | parent/partner/mentor/driver/ambassador require confirm; teen uses magic-link |
| **F25** (e-money license partnership) | CANON-ECON-001/PARENT-001 + ECON-009 | manual rail ships; auto-DH path waits for EP partner signature |
| **F42** (driver UI scope) | CANON-AUTH-008 + LIFE-007 | API-only ships V1; full UI deferred to V1.5 |
| **F46** (ride dispute table) | CANON-LIFE-001 | refund flows use cancelled+refund_id linkage instead of 'refunded' enum value |
| **F54** (Kai persona lock) | CANON-AI-008 | single canonical prompt builds at lib/ai/prompts/roles.ts:KAI_CANONICAL_PROMPT |
