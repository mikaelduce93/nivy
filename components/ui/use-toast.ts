/**
 * Re-export shim for backwards compatibility.
 *
 * Canonical source: `@/hooks/use-toast`.
 *
 * This file existed historically as an exact duplicate of `hooks/use-toast.ts`.
 * It is kept only to avoid breaking any legacy import paths.
 * New code MUST import from `@/hooks/use-toast`.
 */
export * from '@/hooks/use-toast'
